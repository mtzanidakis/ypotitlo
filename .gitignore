# Built binary
/ypotitlo

# Test / scratch subtitle files, and the containers they get extracted from.
# Unit-test fixtures under testdata/ are tracked deliberately — the whole test
# strategy is golden round-trip files.
*.srt
*.vtt
*.ass
*.ssa
*.mkv
*.mka
*.webm
!**/testdata/**

# Test & lint artifacts
coverage.out
gofmt.out

# Local tooling overrides and secrets
/mise.local.toml
node_modules/
